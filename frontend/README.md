# Coffeeroasters Frontend

Built with [SvelteKit](https://kit.svelte.dev/).

## Setup

```bash
npm create svelte@latest .
npm install
npm run dev
```

The frontend communicates with the Go backend API at `http://localhost:8080`.
